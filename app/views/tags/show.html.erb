<h1>Tags:</h1>
<p id="tags">
  <% @tags.each do |tag| %>
    <%= link_to tag.name, tag %>,
  <% end %>
</p>

<p>
  Name:<div id="name" data-par="<%= params[:id] %>"></div>
</p>
<p>
  Tagged Count:<div id="count"></div>
</p>

<div id="event" class="row">
</div>
